iframe id="typeform-full" width="100%" height="100%" frameborder="0" src="https://adhish.typeform.com/to/Xk0UUt"
script type="text/javascript" src="https://s3-eu-west-1.amazonaws.com/share.typeform.com/embed.js"

javascript:
  (function() {
    function saveTypeFormDataToDB() {
      $.ajax({
        url: '/fetch_and_save_current_form',
        method: 'post',
        success: function() {
          window.location.href = 'http://7af61ad1.ngrok.io';
        }
      });
    }

    setTimeout(function() {
      var div = $('<div></div>');

      div.css(
        { width: '30px', height: '30px', "background-color": 'rgb(22, 97, 250)', position: 'fixed', right: '10px', top: '10px', "border-radius": '10px', cursor: 'pointer' }
      );

      div.prop('id', 'magic-button');

      $('body').append(div);

      $('#magic-button').on('click', function() { saveTypeFormDataToDB(); });
    }, 4000) }
  )();
